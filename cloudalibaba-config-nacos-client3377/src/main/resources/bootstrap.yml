server:
  port: 3377

spring:
  profiles:
    active: test
  application:
    name: nacos-config-client
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
        namespace: spring-cloud-test-id
        group: local